# Enterprise Agent Platform Plugin Manifest
# Generated by: eap plugin new __PLUGIN_NAME__
# See: https://docs.example.com/plugins/manifest

# ------------------------------------------------------------------ #
# Identity
# ------------------------------------------------------------------ #

name: __PLUGIN_NAME__
version: 0.1.0
description: __PLUGIN_DESCRIPTION__
author: Your Name
homepage: https://example.com/plugins/__PLUGIN_NAME__

# ------------------------------------------------------------------ #
# Compatibility
# ------------------------------------------------------------------ #

# Minimum EAP platform version required to run this plugin
min_platform_version: 0.1.0

# ------------------------------------------------------------------ #
# Permissions
# ------------------------------------------------------------------ #

# Declare the minimum permissions your plugin needs.
# Users will see this list when enabling the plugin.
#
# Available permissions:
#   agent.read         - Read agent conversations and metadata
#   agent.write        - Modify agent behaviour and inject context
#   document.read      - Read documents from the RAG index
#   document.write     - Write documents to the RAG index
#   webhook.send       - Send webhook events
#   audit.read         - Read audit log entries
#   compliance.read    - Read compliance reports
required_permissions:
  - agent.read

# ------------------------------------------------------------------ #
# Tool definitions
# ------------------------------------------------------------------ #

# Tools registered by this plugin (auto-discovered from register_tools())
# Declare here for documentation and permission control.
tool_definitions:
  - name: __PLUGIN_NAME___example_tool
    description: An example tool provided by __PLUGIN_NAME__.
    parameters:
      type: object
      properties:
        query:
          type: string
          description: The input query for the tool.
      required:
        - query

# ------------------------------------------------------------------ #
# Lifecycle hooks
# ------------------------------------------------------------------ #

# Hooks this plugin listens to.
# The runtime uses this to optimise hook dispatch.
hooks:
  - before_agent_run
  - after_agent_run
